# Proof artifacts

JSON and PNG proofs from Playwright and in-browser self-checks.  
**Current** tracked set lives in **`current/`**.

Harness still writes new `proof-*` files to the **repo root** when you run `node tests/run_selfcheck_playwright.js` (by design).