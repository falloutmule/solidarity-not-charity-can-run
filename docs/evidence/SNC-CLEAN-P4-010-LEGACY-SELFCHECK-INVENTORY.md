# SNC-CLEAN-P4-010 — Isolated legacy selfcheck inventory

Run reviewed: `test-results/selfcheck-runs/phase3-harness-artifact-20260723T2` (2026-07-23), against `main` `2ea454a`.  The run copied the artifact into the selfcheck directory and exercised the historical all-sections harness.  Repeated copies of the same result in `proof-full-selfcheck.json` and `proof-playwright-summary.json` are listed once below.

## Classification key

- **Obsolete historical expectation** — assertion belongs to retired procedural/candidate-era characterization, or assumes a 320x200/dist artifact.  Do not change runtime to satisfy it.
- **Already covered** — behavior remains relevant and has a maintained focused owner test.
- **Useful behavior missing focused coverage** — behavior may still be product-relevant, but this run is not an adequate owner; add a narrow test in a future card.
- **Still-valid behavior** — current behavior contract remains applicable; the focused owner (if any) is named.

## Assertion inventory (unique failures)

| Legacy section / assertion(s) | Source owner | Classification and recommended action |
|---|---|---|
| `buildingModuleFacade`: D2 storefront module; face-role lookup | `src/js/game-22-section-13-main-loop.js` (`runBuildingModuleFacadeSelfCheckBody`), procedural generation in `src/js/game-09-section-3-city-generation.js` | **Obsolete historical expectation.** Procedural facade sampling is not the current authored/bitmap acceptance contract. Use `tests/bitmap_building_renderer_verify.js` and `test:custom-next` for current buildings. |
| `buildingScalePolish`: D1 pavilion larger/substantial | `game-22-section-13-main-loop.js` (`runBuildingScalePolishSelfCheckBody`) | **Obsolete.** Retired procedural pavilion visual target; no runtime change. |
| `d1ParkLandmark`: restroom/gazebo landmark; `earlyDistrictProgression`: park/plaza/open spine; `levelSelector`: D1 park landmark | `game-22-section-13-main-loop.js`; authored runtime `src/js/game-09a-authored-level-runtime.js` | **Obsolete historical expectation.** Current D1 is the locked authored level. Current correctness is covered by `tests/authored_d1_level_verify.js`, `tests/authored_d1_ordinary_game_smoke.js`, and `tests/authored_d1_save_load_verify.js`. |
| `facadePackBridge`: D2 storefront role; D3 side/back role | `game-22-section-13-main-loop.js` (`runFacadePackBridgeSelfCheckBody`) | **Obsolete.** Historical procedural role-hit probes; current bitmap contract is owned by `tests/bitmap_building_renderer_verify.js`. |
| `facadePackV2Safe`: D2 boarded_shop_3x2; D3 garage_service_4x2; boarded/garage facade hits | `game-22-section-13-main-loop.js`; `game-09-section-3-city-generation.js` | **Obsolete historical expectation.** Module registry is retained for compatibility, but this candidate-era probe is not a release gate. |
| `fpvGroundPlaneAlignment`: debugHelper, D1/D2/D3 bases, shared floor, mass-upward, D1 identity | `src/js/game-22-section-13-main-loop.js`; procedural helpers in `src/js/game-15-section-6-procedural-assets.js` | **Obsolete characterization.** It assumes generated city placements and a procedural landmark. Do not “repair” source from this result. |
| `buildingSmoothStyle`, `continuousFacadeTexture`, `facadefinal-readability`: NPC grounding, wall-floor alignment, D2/D3 proof targets, side/back, D1 identity | `game-22-section-13-main-loop.js` selfcheck bodies | **Obsolete historical expectation.** These are superseded visual-candidate checks. Current renderer proof is `tests/bitmap_building_renderer_verify.js`; ordinary authored D1 smoke remains the gameplay gate. |
| `facadeArtVocabulary`, `facadeCompositionReadability`: D1 identity, D2 storefront/boarded, D3 garage/side-back role/debug hits | `game-22-section-13-main-loop.js` | **Obsolete.** Procedural facade-art acceptance from retired cards; preserve as archive evidence only. |
| `facadeskins1`: column draw, registry profile, live skin id/role/span/u/debug/variant | `src/js/game-09c-section-facadeskins1-bitmap-facades.js`; `game-22-section-13-main-loop.js` | **Obsolete candidate probe.** `BUILD_ID=chromeinput2` does not activate the `facadeskins1` route. Use current bitmap/custom-next tests. |
| `walltextures2`: `oneMaterialPerBuilding` | `game-22-section-13-main-loop.js`; `game-09-section-3-city-generation.js` | **Obsolete.** Retired procedural single-material city assertion. |
| `walltextures3`: component material at seed 880101 / all building walls component-owned | `game-22-section-13-main-loop.js` | **Obsolete.** Seeded procedural-generation invariant; no current authored-level requirement. |
| `walltextures4`: single-material ownership, walltextures2 compatibility, raycaster invariant | `game-22-section-13-main-loop.js` | **Obsolete aggregate.** Depends on retired `walltextures2/3` probes. Current raycaster/interpolation checks are separate maintained tests (`test:farfield-*`, `test:render-interpolation`). |
| `decalIntegration1`: walltextures3/walltextures4 preservation, sign/commercial decals | `game-22-section-13-main-loop.js` | **Obsolete historical expectation.** Procedural generated-city decals are not current product scope. |
| `decalIntegration2`: walltextures3/4 and decalIntegration1 compatibility | `game-22-section-13-main-loop.js` | **Obsolete aggregate.** Retired candidate chain; no source action. |
| `decalIntegration3Visual`: prior decal compatibility and storefront assembly | `game-22-section-13-main-loop.js` | **Obsolete aggregate.** Historical visual chain; do not promote to release gate. |
| `decalIntegration4MaterialIdentity`: no-grey-flip, facade layouts, walltextures3 ownership | `game-22-section-13-main-loop.js` | **Obsolete aggregate.** Historical material/decal candidate. |
| `streetReadabilityMinimap`: limited navigation palette; D1 pavilion | `game-22-section-13-main-loop.js` | **Obsolete expectation.** Pavilion dependency is procedural-era. Minimap behavior is indirectly exercised by authored D1 smoke; add a focused palette test only if product requirements reintroduce it. |
| `raycaster-invariant`: internal resolution must be 320x200 | `game-22-section-13-main-loop.js`; resolution source `src/js/game-21-section-12-render-resolution.js` | **Obsolete historical expectation.** Production stop condition explicitly retains 400x250. Current far-field resolution/projection tests are the owners. |
| `release-artifact`: artifact must be root `index.html` or `dist/index.html` | `tests/run_selfcheck_playwright.js` summary assertion | **Obsolete harness assumption.** The isolated run’s requested artifact is `test-results/.../index.html`; release contract is root `index.html`, verified by `test:build-proof-routing` / metadata truth. Fix harness routing in a future cleanup card, not runtime. |
| `singleMaterialBuildingTextures`: all four materials, raycaster/world adapters, sprite occlusion, fixed-step | `game-22-section-13-main-loop.js` aggregate | **Obsolete aggregate.** It chains retired walltexture checks. Current owners are `tests/bitmap_building_renderer_verify.js`, `test:render-interpolation`, and authored D1 tests. |
| `world-adapter-collision/render`: `raycasterInvariantPasses`, movement-collision/sprite-occlusion dependent checks; `sprite-occlusion-screenshot`: `raycasterInvariantPasses`; `fixed-step-simulation`: `raycasterInvariantStillPasses` | `game-22-section-13-main-loop.js` selfcheck adapters | **Already covered (dependency failure only).** The underlying collision, sprite occlusion, and fixed-step checks are green; only the stale 320x200 raycaster invariant is false. Keep `tests/bitmap_building_renderer_verify.js`, `test:render-interpolation`, and authored smoke as owners. |
| `fullRunProgression`: `mobile regression` | `tests/run_selfcheck_playwright.js` (`fullRunProgressionSection`), runtime helper in `game-22-section-13-main-loop.js` | **Already covered / harness-sensitive.** Broad historical run is not a release gate; authored D1 smoke and save/load focused tests cover current progression. Retain this failure as characterization, investigate only with a reproducible narrow test. |
| `visualRectangleRegression`: full-run progression and save/load regression | `tests/run_selfcheck_playwright.js` (`visualRectangleProofShots`) | **Already covered.** Current save contract is owned by `tests/authored_d1_save_load_verify.js`; visual renderer contract by `tests/bitmap_building_renderer_verify.js`. No action from this aggregate failure. |
| `mobileControlReliability`: MENU open/resume control state bad; `movementCollision`: mobile control reliability regressed | `tests/run_selfcheck_playwright.js`; input/menu source `src/js/game-06-section-2b-mobile-touch-input.js` and `game-07-section-2c-responsive-mobile-menu-html-overlay.js` | **Useful behavior missing focused coverage.** Pointer path is covered by `tests/chrome_pointer_path_verify.js` and layout/cache by `tests/mobile_ui_cache_verify.js`, but menu open/resume lifecycle is not isolated. Create one focused lifecycle test before any runtime change. |
| `declarativeControls`: reload did not preserve custom size | `game-22-section-13-main-loop.js` (`runDeclarativeControlsSelfCheckBody`) | **Useful behavior missing focused coverage.** Persistence/reset is a real controls contract, but the legacy harness reload is timing-sensitive. Add a narrow storage/reload test; do not alter controls based solely on this run. |
| `harnessIsolation`: full self-check failed inside isolation | `tests/run_selfcheck_playwright.js` isolation wrapper | **Aggregate only.** It repeats the failures above; no independent product assertion. |

## Triage conclusion

The failures are dominated by retired procedural facade/decal/texture candidate checks and stale 320x200/artifact-path assumptions. The only actionable coverage gaps identified are menu open/resume lifecycle and declarative-control custom-size persistence; both require focused tests before implementation. Current authored D1, bitmap renderer, pointer-path, far-field, interpolation, metadata, and save/load owners remain the release truth.
