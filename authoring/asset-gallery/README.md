# SNC Asset Gallery

`assets.json` is the canonical visual-production registry. Source art remains beside its own metadata; generated gallery output is ignored under `test-results/asset-gallery/`.

Run `npm.cmd run gallery:validate`, then `npm.cmd run gallery:build`. The gallery is development-only and must not modify the playable root artifact.
