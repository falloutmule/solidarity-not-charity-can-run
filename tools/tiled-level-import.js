'use strict';
const fs = require('fs'); const path = require('path'); const { ROOT, loadDistrict01, fromTiled, semanticEqual } = require('./tiled-level-bridge');
function main(argv) { const [input] = argv; if (!input) throw new Error('usage: node tools/tiled-level-import.js <map.tmj>'); const baseline = loadDistrict01(); const imported = fromTiled(JSON.parse(fs.readFileSync(input, 'utf8')), baseline); const pass = semanticEqual(imported, baseline); const output = path.join(ROOT, 'test-results', 'tiled-import', 'district-01-import.json'); fs.mkdirSync(path.dirname(output), { recursive: true }); fs.writeFileSync(output, `${JSON.stringify(imported, null, 2)}\n`, 'utf8'); if (!pass) throw new Error('District 1 Tiled import differs from the current authored companion source'); process.stdout.write(`${JSON.stringify({ pass: true, output: path.relative(ROOT, output).replace(/\\/g, '/') })}\n`); }
try { main(process.argv.slice(2)); } catch (error) { console.error(error.message); process.exitCode = 1; }
